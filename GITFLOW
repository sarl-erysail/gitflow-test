1. Create a repository on GitHub
2. Clone this repository with "git clone <url to repo>". This clones the repository into a directory and sets up the remote repo as origin
3. Make a master branch with "git checkout -b master"
4. Make a develop branch with "git checkout -b develop"
5. Before branching off master or develop make sure that the base branch is recent with "git checkout <base>" and "git pull origin <base>"
6. Make a feature branch to do work in. Use a command like in steo 3 and 4 and name the brwnch with the "feature/" prefix
7. Do work in this branch and add and commit your work
8. Regularly incorporate changes in the develop branch with "git fetch" and "git rebase -i origin/develop"
9. When done "git push"
